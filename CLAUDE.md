# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This is a new project directory within the AutomatIA monorepo (`automatia-proyectos/Lo de Juan/`). No code has been added yet.

## Monorepo Context

This project lives under the AutomatIA umbrella — Xabier's freelance digital-services work (web, automation, IoT, firmware) targeting local businesses in Donostia. See the root `CLAUDE.md` at `autonomia/CLAUDE.md` for the full monorepo overview and other sub-projects (Parques, el angulero, automatia-web).

## MCP Tools Available

This workspace has the `automatia` MCP server enabled, which provides project management tools:

- `ver_panel` — dashboard overview
- `listar_clientes`, `crear_cliente` — client management
- `listar_proyectos`, `crear_proyecto` — project management
- `listar_facturas`, `crear_factura` — invoice management
- `crear_presupuesto` — quotes/estimates
- `listar_gastos`, `crear_gasto` — expense tracking
- `registrar_horas` — time tracking
